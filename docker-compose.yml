services:
  api:
    build: ./api
    ports:
      - "3000:3000"
    restart: unless-stopped

  web:
    build: ./web
    ports:
      - "8000:8000"
    depends_on:
      - api
    restart: unless-stopped
